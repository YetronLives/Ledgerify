# Server Configuration
PORT=5000

# Database Configuration (use your own credentials)
DB_USER=ledgerify_user
DB_PASSWORD=your_postgres_password_here    # ← generate with: openssl rand -base64 32
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ledgerify_db

# Authentication (MUST be generated)
JWT_SECRET=your_jwt_secret_here            # ← generate with: openssl rand -base64 64